name: tabangnow_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  flutter_secure_storage: 10.3.1
  file_picker: 10.3.10
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  path_provider: ^2.1.6
  open_filex: ^4.7.0
  geolocator: ^14.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
